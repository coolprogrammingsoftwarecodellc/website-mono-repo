language: ruby
before_install:
  - curl -o- https://raw.githubusercontent.com/creationix/nvm/v0.33.5/install.sh | bash
  - nvm install 6
install:
  - npm install
deploy:
  - provider: script
    script: node deploy .
